<% include header %>
<h1>List user</h1>
<p><%=: users | map: 'name' | join %></p>
<ul id="user">
    <% users.forEach(function(user) { %>
       <li><%= user.name %> &lt; <%= user.email %> &gt;</li>
    <% })%>
</ul>
<% include footer %>
